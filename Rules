#!/usr/bin/env ruby

# 1. Matches items have `html` extension
# 2. Matched items will be laid out using the default layout
compile '/**/*.html' do
  layout '/default.*'
end

# This is an example rule that matches Markdown (.md) files, and filters them
# using the :kramdown filter. It is commented out by default, because kramdown
# is not bundled with Nanoc or Ruby.

compile '/**/*.md' do
  filter :kramdown, input: 'GFM'
  filter :colorize_syntax, :default_colorizer => :pygmentsrb
  layout '/default.*'
end

# For `html` or `md` files,
# IF it is `index.something`, then it can be accessed by `/` or `/index.html`
# ELSE
# It is `/item.identifier.without_ext/(index.html)`
route '/**/*.{html,md}' do
  if item.identifier =~ '/index.*'
    '/index.html'
  else
    item.identifier.without_ext + '/index.html'
  end
end

compile '/**/*' do
  write item.identifier.to_s
end

layout '/**/*', :haml
